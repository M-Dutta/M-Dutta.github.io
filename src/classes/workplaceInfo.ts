class WorkplaceInfo {
    name: string;
    infoFile: string;

    constructor(name: string, infoFile: string) {
        this.name = name
        this.infoFile = infoFile
    }
}

export default WorkplaceInfo;
